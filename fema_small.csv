import pandas as pd

df = pd.read_csv("fema_sample.csv")

# SAMPLE down to ~10,000 rows (or less if needed)
smaller = df.sample(n=10000, random_state=42)

smaller.to_csv("fema_small.csv", index=False)

print("New file saved:", smaller.shape)
